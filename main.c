#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#define MAX_STR 50
#define SIZE_QUEUE 3

struct nodo{
    char nom[MAX_LEN_STR];
    float precio:
    struct nodo *prev, *next;
}

enum boolean ={falso, verdad};

int main(){
    menu();
    return;
}